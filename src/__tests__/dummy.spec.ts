describe('dummy describe test', () => {
  beforeAll(async () => {
    console.log('beforeAll');
  });

  describe('Create', () => {
    it('should create a user', async () => {
      expect(1).toBe(1);
    });
  });
});
